export const constants = {
    mine:           10,
    explodedMine:   11,
    flagged:        13,
    apiUrl:         `http://${window.location.hostname}:8080/api/v1/minesweeper`, // FIXME: real URL
    wsUrl:          `http://${window.location.hostname}:8080/ws` // FIXME: real URL
};